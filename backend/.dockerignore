# Backend Docker ignore - reduce build context size and exclude local artifacts

# Version control
.git
.gitignore

# Environment files (keep secrets out of images)
.env
.env.*

# Build artifacts and temporary files
tmp
tmp/**
/tmp
/tmp/**
bin
bin/**
dist
build
coverage
coverage.out

# Go build/cache directories (these can be large; modules are handled via go mod download)
pkg
pkg/**
**/vendor
vendor/**

# Node/npm (if frontend lives in repo; prevent nested node_modules from being sent)
node_modules
**/node_modules

# IDE/editor metadata
.vscode/
.idea/
*.sublime-workspace
*.sublime-project
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Logs and lockfiles not needed in image
*.log
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Optional: ignore Dockerfile builds or local container artifacts if any
.dockerignore
